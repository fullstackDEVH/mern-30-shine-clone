import React, {FC} from 'react';

interface IProps {

};

const ServicesScreen: FC<IProps> = ({ }) => {
    return (
        <>
            ServicesScreen
        </>
    )
};

export default ServicesScreen;